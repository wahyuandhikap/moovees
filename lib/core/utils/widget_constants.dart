import 'package:flutter/material.dart';

const mainColor = Colors.deepOrange;

final defaultBoxShadow = BoxShadow(
  color: Colors.black.withOpacity(0.05),
  spreadRadius: 1.5,
  blurRadius: 3,
  offset: const Offset(0, 0),
);
